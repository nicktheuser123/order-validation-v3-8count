# Environment variables
.env
.env.local
.env.*.local

# Dependencies
node_modules/
npm-debug.log*
package-lock.json

# Testing
test-results.md
.jest-test-steps.json
e2e-gp-testing/results/
.playwright-cli/
# Playwright accessibility-tree snapshots auto-saved to .playwright-cli/;
# never commit manual copies an agent may have piped to the repo root.
order*-*.yml

# MVP deterministic-runner transient artifacts
# (discovery-<flow>.json files are tracked — they're the authoritative
# observation logs the runner depends on, not scratch output.)
e2e-gp-testing/heal.log

# Claude Code internals
.claude/scheduled_tasks.lock
e2e-gp-testing/.order-*-done.json
e2e-gp-testing/.setup-done-*
e2e-gp-testing/.worker-common.md

# IDE and editor files
.vscode/
.idea/
*.swp
.DS_Store

# Logs
logs/
*.log
