# MVP Deterministic Runner — Handoff Status

**As of 2026-04-22.** Session handoff for continuing the "deterministic Playwright-cli with LLM fallback" MVP. Read this + the files listed at the bottom before doing anything.

## Where we are

**Round 1 MVP is complete and working end-to-end.** Order #3 (Standard × 3 + Premium × 2, Guest checkout, no promo) placed deterministically with `$383.40` total, zero LLM API calls, ~80s wall time.

- Runner: `e2e-gp-testing/scripts/run-order-deterministic.js` — hardcoded for Order #3, no CLI args, no order-spec file yet (intentional).
- Approach: Bubble-side clicks use CSS IDs (captured in discovery pass); Authorize.net form uses `run-code` + `pressSequentially`.
- Discovery output: `e2e-gp-testing/discovery.json` — captured by an LLM agent completing one Order #3 while probing DOM.

## Why this matters (context)

The earlier LLM-agent runbook-driven run took ~23 min wall / ~$25–35 because Opus reasoned through an accessibility-tree snapshot per click. Bubble's `ID Attribute` editor field produces stable DOM `id="..."` values, so most clicks become pure Playwright selectors (0 LLM cost). LLM agents stay as a safety net on selector-miss, not the primary driver.

## Approved plan

See `/Users/abhishekjoseph/.claude/plans/does-this-contain-the-glowing-rabbit.md` — the MVP scope was tightened from 20 orders to 1 order (Order #3 only), and the user explicitly rejected act.js/Haiku shims, YAML DSL, and verb files for round 1. "No extra shit" was the directive.

## Files the next Claude must read first

1. `/Users/abhishekjoseph/.claude/plans/does-this-contain-the-glowing-rabbit.md` — approved plan, deviations logged
2. `e2e-gp-testing/runbook.md` — full Phase 2 flow + pitfalls (Authorize.net pressSequentially, ionic-toggle inner-`<label>`, etc.)
3. `e2e-gp-testing/discovery.json` — business-name → CSS-ID mapping for Order #3's path
4. `e2e-gp-testing/scripts/run-order-deterministic.js` — the working runner; read its helpers (`pw`, `pwEval`, `waitFor`, `fillBubbleInput`, `clickVisibleByIdIndex`) before generalizing
5. `e2e-gp-testing/settings.json` — payment card, ticket types, promotions, guest users, verification.waitAfterOrderMs
6. `e2e-gp-testing/e2e-state.json` — event URL, event ID, ticket-type IDs, promo IDs

## Gotchas already solved — do NOT re-debug these

Moved to `runbook.md` → **Gotchas Registry** (grep the registry by component name: `authorize`, `ionic`, `bubble input`, `bubble shared IDs`, `playwright-cli`, `unique contact email`). The 9 entries that used to live here are now indexed there with Symptom / Cause / Fix / Source. `CLAUDE.md` enforces that you must grep + cite the registry before writing deterministic Playwright code.

## Open items for round 2

(deferred from MVP — user said "iteratively, we can improve in the second round based on learning")

- [ ] **LLM fallback wiring** — if deterministic run fails at any step, spawn the existing runbook-driven agent to finish that order. No Haiku, no new shim files — just invoke the same Agent tool pattern with `playwright-cli`. See the plan's "Step 5 — Fallback wiring".
- [ ] **Generalize the runner** — extract the Order #3 hardcoded flow into parameterized functions driven by `orders-spec.json` (flat table: `orderNumber`, `tickets: {type: qty}`, `promo`, `checkout`, `specialChars`, `expectedTotal`, `userCredentialKey`). Support the other 4 checkout flows (Logged-in, Guest→Register, Guest→Login top-right, Register→Login) and promo codes. First extend to orders with no auth (#1, #2, #3 already done, #4, #5, #11, #12).
- [ ] **Missing IDs to ask user about** (recorded in `discovery.json` → `missingIdsOnStep1`): authorized-cardholder checkbox, cart total/subtotal display (needed for "wait for subtotal updated" settling), cart item-count heading, cart countdown timer. Step 2 is missing the Order Total display id too.
- [ ] **`run-all.js` driver** — preflight → loop orders → reconcile → Jest. This is what the plan's Step 5 describes.
- [ ] **Jest validation** — once `state.orders` has real orderIds from a deterministic run, `npm test -- e2e-gp-testing/tests/e2eOrder.test.js` should hit 41/42 same as before. Haven't run it yet because round 1 was just Order #3 solo.
- [ ] **Add pacing knobs to settings.json** — `pacing: { minOrderMs, jitterMinMs, jitterMaxMs }` currently hardcoded in the runner.
- [ ] **Bump `verification.waitAfterOrderMs` 30000 → 60000** — deferred until multi-order run exposes backend-settle race conditions.

## How to resume

In a fresh Claude Code session, first prompt should be something like:

> Read `e2e-gp-testing/.mvp-status.md` and the files it references. Then [your next instruction, e.g. "generalize the deterministic runner to also handle orders #1, #2, #4, #5, #11, #12 (all guest, no-auth orders)"].

## Known-good Bubble orders from this MVP

| When | Order ID | Total | Status | Path |
|---|---|---|---|---|
| Discovery agent run | `1776848611277x759673563772354600` | $383.40 | Paid | LLM-driven |
| Deterministic run (final) | `1776857578441x218399742233411600` | $383.40 | Paid | Deterministic |

Debris (Pending orders created during Authorize.net debugging — user may choose to delete):
- `1776849484138x586289631171706900` ($360 Pending) — wrong authnet selectors run
- plus any other `abhishek+gp-det-o03-*@millionlabs.co.uk` with status Pending
